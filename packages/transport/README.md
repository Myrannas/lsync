# @lsync/transport

Shared MessagePack wire contracts and transport helpers for lsync.

```sh
pnpm add @lsync/transport
```

Most applications use this package transitively through `@lsync/client` or `@lsync/server`.
